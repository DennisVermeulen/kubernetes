releases:
  - name: awx
    namespace: awx
    # https://github.com/kubernetes/charts/tree/master/stable/keycloak
    chart: stable/awx
    values:
      - awx:
          ingress:
            enabled: true
            path: /
            hosts:
